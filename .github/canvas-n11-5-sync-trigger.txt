Temporary trigger for repository-pinned N11.5 validation. Do not merge.
Renderer-root Attempt: 12 — restore the official rc.8 Web→ui-renderer project reference on top of the client build-environment ambient, then validate formal Host→Client→static→regressions→invariants order; gated push only after every gate passes.