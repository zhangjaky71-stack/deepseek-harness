# N11.5 — Harness `0.1.1-rc.2` Upstream Realignment Gate

Status: `BLOCKED / REOPENED`

> 文件名保留 `rc8` 只是为了不破坏既有链接；本文件的当前目标已经不是 rc.8。

## 1. Goal

在继续 N16–N25 前，把私有 Canvas 栈重新落到官方最新 infrastructure seam 上，并明确保留最小产品级 divergence。

Official target:

```text
deepseek-ai/deepseek-harness
b150a551b8d465e31e418e1b2eaf5e79bbb7d28e
dsh@0.1.1-rc.2
```

## 2. What rc.8 work remains useful

PR #40/#41 的以下工作仍是有效 prior implementation：

- dynamic `ui-renderer` owns application root；
- Web boot framework-free；
- `window.__ModuleLoader__` bootstrap facade；
- parser-preloaded modules/runtime bootstrap；
- Canvas dynamic `shell.main` product slot；
- ui-canvas client runtime boundary收窄。

但它们只证明当时的 rc.8 compatibility，不证明 0.1.1-rc.2 final compatibility。

## 3. New upstream changes that reopen this node

### A. Session Projection — P0

Migrate Canvas projection from private owner/readGuard assumptions to official Host state + optional wire view definitions. Re-map N04 authorization to current official exposure seam.

### B. Attachment/image pipeline — P0

Synchronize official normalized master/request-image APIs. Canvas must rely on official Attachment for image bytes and must not implement a second request transform/cache.

### C. Settings Describe Mirror — P0

Replace private per-scope describe/read lifecycle with official shared mirror-derived namespace scopes while keeping N09 Host restart-applied semantics.

### D. ui-renderer React bindings — P0

Official renderer absorbed React bindings; legacy `web-react` compatibility assumptions must be retired/reconciled. Web/Layout must not regain React root ownership.

### E. Web transport — P0

Adopt `globalThis.__DSH_TRANSPORT__.loadBundle` support and suppress HTTP prefetch when transport owns bundle bytes.

### F. Command image envelope — integration

N18 should consume official command submission image envelope. N11.5 must ensure the synchronized client/command/attachment graph exposes the current contract.

### G. Build/gate surface — release

Use current `scripts/build.ts` and current repository gates/coverage/client-domain/runtime-closure/doc checks.

## 4. Intentional private compatibility retained

### `shell.main`

Official latest layout does not provide Canvas main product seat. Private product requires:

```text
sidebar | shell.main(Canvas) | conversation | details
```

N11.5 must **not** solve upstream sync by deleting Canvas or letting Conversation retake the only product center. Instead:

1. start from latest official `ui-layout`;
2. replay the minimal generic `shell.main` slot/geometry patch;
3. keep ui-layout free of Canvas domain imports/business state;
4. test the divergence explicitly.

## 5. Required migration checklist

### Official infrastructure

- [ ] synchronize Session Projection sources/contracts;
- [ ] migrate Canvas projection state/wire view;
- [ ] synchronize Attachment/attachment-local image pipeline;
- [ ] migrate Canvas stable image ref usage;
- [ ] synchronize Settings mirror/binder behavior;
- [ ] migrate ui-canvas Settings binding;
- [ ] synchronize ui-renderer React bindings; retire obsolete web-react compatibility;
- [ ] synchronize Web transport `loadBundle` behavior;
- [ ] synchronize command image-envelope dependencies relevant to N18;
- [ ] synchronize root build/gate definitions.

### Replay Canvas extensions

- [ ] replay `shell.main` over latest official layout;
- [ ] re-bind `ui-canvas` to latest session/renderer/settings/remotes;
- [ ] preserve Minimal/Editor session-local presentation lifecycle;
- [ ] preserve Conversation/Composer ownership;
- [ ] preserve open-world node catalog/Editor behavior.

### Revalidate downstream existing implementations

- [ ] N01–N11 affected seams reviewed;
- [ ] N12 Engine asset boundary reviewed;
- [ ] N13 Media Model Registry remains separate from Chat LLM model routes;
- [ ] N14 image materializer contract targets official Attachment;
- [ ] N15 admission contract uses current upstream auth/feature/provider seams and exact WorkflowRef。

## 6. Generated artifacts

After source reconciliation only:

- [ ] regenerate `pnpm-lock.yaml` with pinned pnpm;
- [ ] regenerate Typert outputs;
- [ ] regenerate client/module/config/tool/persistence/scoped-event catalogs/docs;
- [ ] refresh translation pairings with owner script.

No manual fabrication.

## 7. Source/REAL tests

At minimum prove:

- official Projection replay/checkpoint + Browser wire view;
- read authorization at current exposure boundary;
- shared Settings mirror + Canvas restart settings;
- Attachment normalized/request-image focused tests;
- renderer root/binding lifecycle;
- transport-owned loadBundle boot;
- latest layout + `shell.main` intentional fork;
- real Canvas + Conversation assembled Web;
- ui-canvas plugin dispose/HMR cleanup;
- current N10–N15 built package graph remains loadable after synchronization.

## 8. CI infrastructure blocker rule

Existing stacked PRs have shown jobs failing before repository steps (`steps=[]`/`steps=null`, missing Azure logs/`BlobNotFound`) or staying queued. This is neither PASS nor product failure. N11.5 remains blocked until exact-head repository-owned checks actually execute.

## 9. N12–N15 inheritance rule

Existing implementations may be preserved and adapted while N11.5 is open, but they cannot claim a final synchronized runtime baseline. Before N16 starts, N11.5 must close all P0 infrastructure items above or N16 would encode new Run lifecycle against obsolete client/session/asset seams.

## 10. Acceptance

N11.5 is accepted only when both conditions hold:

1. private source is mechanically aligned with official 0.1.1-rc.2 infrastructure plus documented minimal Canvas extensions；
2. generated outputs and REAL repository/runtime evidence exist on the exact private post-sync head。

Until then:

```text
BLOCKED / REOPENED
```
