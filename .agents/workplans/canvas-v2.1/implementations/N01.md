# N01 implementation record

- Node: N01 — Canvas Domain, type system, and state invariants
- Branch: `agent/canvas-n01-domain`
- Pull request: #8
- Status: REVIEW

## Implemented

- `@deepseek-ai/dsh-canvas` package manifest and package TypeScript project.
- Branded Canvas, workflow, node, edge, run, variant, and video-asset ids.
- Semantic `MediaWorkflow`, node, edge, run, output, asset-reference, and `CanvasSnapshot` types.
- Independent `workflowRevision` and `runRevision` semantics.
- Product-state derivation for EMPTY, READY, DIRTY_READY, RUNNING, COMPLETED, FAILED, CANCELLED, and INTERRUPTED.
- Runtime domain invariants for ids, revisions, timestamps, workflow relationships, run lifecycle, output selection, durable asset metadata, and JSON-safe workflow configuration.
- Unit coverage for the N01 acceptance examples.
- Package/group bilingual READMEs, package invariant companion, root package-group index entry, and a proposed Canvas Agent Note.
- Canvas source-resolution facade entries in `tsconfig.base.json`.
- Canvas project reference in the Host aggregate `tsconfig.host.json`.

## Validation completed

- A strict TypeScript structural check using the available local compiler passed against dependency stubs.
- An emitted-JavaScript domain smoke passed for READY, DIRTY_READY, and binary-config rejection.
- Export JSDoc was normalized to the repository-required `@param` / `@returns` form.
- Bilingual hash records for the package README and root package index were refreshed from committed Git blob ids.
- The new package is registered in both the source-resolution facade and the Host TypeScript aggregate.

## Repository validation blocker

GitHub Actions currently fails before allocating runners for multiple jobs: repository CI jobs complete with zero executed steps and no job log, and a temporary `ubuntu-latest` N01 bootstrap job showed the same zero-step failure twice. The same pattern is present on the documentation-only base PR, so there is no test failure output attributable to Canvas.

Because this environment cannot clone the private repository or resolve the npm registry, it cannot run the repository-pinned pnpm 11.7.0 toolchain. The following generated/validation work therefore remains before N01 can move from REVIEW to ACCEPTED:

- generate the `packages/canvas/canvas` importer in `pnpm-lock.yaml` with pnpm 11.7.0;
- regenerate `docs/module-graph.md` after the workspace lock is current;
- run the focused Canvas unit test plus repository package/document/static/coverage gates with the pinned workspace install.

The temporary bootstrap workflow was removed and its trigger PR #9 was closed after runner allocation failed twice; no temporary CI mechanism remains in PR #8.

## Acceptance conclusion

N01 remains `REVIEW`. Its source implementation and TypeScript aggregate integration are uploaded, but the repository's frozen-lockfile and generated-document requirements cannot be truthfully marked complete until the pinned pnpm toolchain can execute. Once the runner infrastructure is available, the remaining work is mechanical lock/module-graph generation followed by repository gates; no additional N01 domain design is currently pending.
