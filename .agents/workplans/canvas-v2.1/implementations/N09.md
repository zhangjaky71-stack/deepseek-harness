# N09 Implementation — Canvas deployment feature policy and Harness settings authority

Status: REVIEW  
Date: 2026-08-22  
Branch: `fix/canvas-n09-v2.2-settings-authority`  
Base: `fix/canvas-n08-v2.2-interaction-context`

## Existing N09 capability implementation retained

The predecessor stack already contains the core N09 deployment policy:

- client-safe capability vocabulary for `canvas`, `editor`, `history`, `video`, `variants`, `partialRun`, `regionEdit`, and `providerFallback`;
- `CanvasFeatureService` / `ctx.canvasFeatures` and the read-only `canvasFeatures` Typert namespace;
- Schemastery `Config` defaults: Canvas/Editor/History enabled; Video/Variants/Partial Run/Region Edit/Provider Fallback disabled;
- parent folding under `canvas.enabled`;
- Host checks for Canvas writes, Browser Editor/layout mutations, History access, initial variant identity, region interaction, and future executable-workflow admission;
- historical disabled Video workflows/results remain readable while new disabled Video use and execution are rejected;
- Browser Canvas publication fails closed on Host capability discovery; Editor and Region UI degrade independently;
- shipped base composition mounts the feature service before dependent Canvas consumers;
- focused Host/UI/REAL-Remote coverage and bilingual feature-policy documentation.

The historical `agent/canvas-n09-feature-flags` branch is no longer a valid continuation base: relative to the remediated N08 stack it is 193 commits behind and zero commits ahead. Its functional work has already been subsumed into later branches. This remediation therefore starts from N08 and changes only the missing settings-authority boundary.

## Gap found during rc.8 review

The prior N09 record described the Schemastery Cordis plugin `Config` as deployment truth, but the current repository has a formal settings authority:

```text
ctx.settings.register(namespace, schema, { base, applies })
  schema defaults -> composition base -> durable user document
```

Browser configuration surfaces consume the same namespace through `ctx.settingsScope.bind()`. Feature-owned settings UI contributes through `settings.section`; the settings shell does not own feature policy.

Without this integration, Canvas had a correct feature service but did not satisfy the N09 workplan requirement to reuse Harness settings persistence/schema authority.

## Remediation delivered

### Host settings namespace

`CanvasFeatureService` now exports the Host namespace identity `canvas`, keeps its existing Schemastery `Config` as the schema/default contract, and treats its Cordis entry config as the composition base.

The service optionally uses `ctx.inject(['settings'], ...)` to register:

```text
settingsNamespace('canvas')
CanvasFeatureService.Config
base = compositionConfig
applies = 'restart'
```

Settings remains optional. A custom/lightweight composition with no settings provider continues to use the entry config alone. When a provider is present, the resolved user document is sampled for that feature-service activation. Removing the settings provider drops its namespace registration and the service falls back to the composition layer; a later provider mount re-registers and samples that provider's section.

### Restart semantics

N09 deliberately keeps deployment feature policy restart-applied. A settings document edit is durable immediately, but it does not silently rewrite the capability surface of an already-running Host. The current activation continues to expose and enforce its sampled `ctx.canvasFeatures.capabilities`; a later Host start or feature-service remount resolves the updated user layer.

This avoids adding a partial live-reconfiguration protocol to N09. Live capability replacement would also require atomic Canvas surface teardown/republication, editor catalog refresh, prompt-preparation replacement, in-flight admission semantics, and future Tool advertisement updates. Those mechanics are not introduced implicitly by a checkbox.

### Browser Canvas settings surface

`ui-canvas` now optionally integrates with the current settings UI contract without making settings a requirement for Canvas rendering:

- it waits on `settingsScope + connection + remote` only for the settings contribution;
- it binds namespace `canvas` through `ctx.settingsScope.bind<CanvasFeatureConfig>()`;
- it registers a feature-owned `settings.section` entry with id `canvas`, independent from `remote.canvasFeatures` and independent from `canvas.enabled`;
- the section renders all eight feature flags, raw user-override versus inherited state, read-only/unavailable states, and explicit restart-required copy;
- writes route only through `SettingsScope.set(feature, { enabled })`; Reset uses `SettingsScope.unset(feature)` so the value re-inherits composition/schema layers;
- reactive scope state is passed through the slot `inject.hooks` compartment and framework-generated `useSettings`; the React component owns no subscription machinery or service object;
- the current Canvas product surface still uses only the Host `canvasFeatures` Remote and never treats the Settings checkbox as current-runtime capability authority.

The `@deepseek-ai/dsh-client-ui-settings` peer is optional and type-only at runtime composition boundaries. Standard Settings integration is therefore additive rather than a new ui-canvas boot dependency.

## Tests added in this remediation

### Host settings integration

`packages/canvas/canvas/tests/feature-settings.spec.ts` covers:

- entry-config behavior when no settings provider is installed;
- `canvas` namespace registration with `applies: restart`;
- schema defaults/composition base/user-layer resolution;
- redacted descriptor with no Canvas secrets;
- durable settings edits not changing current activation capabilities;
- feature-service remount sampling the edited user section;
- namespace cleanup on owner disposal and safe re-registration.

### Browser settings presentation and lifecycle

`packages/client/ui-canvas/tests/settings.client.spec.tsx` covers all eight switches, restart copy, override/inheritance presentation, read-only state, and remote/unavailable settings state.

`capability-gate.client.spec.ts` additionally proves that `canvas.enabled=false` suppresses `shell.main` while the Canvas `settings.section` remains registered, that writes reach `SettingsScope.set/unset`, and disposal removes the settings contribution.

## Security and authority conclusions

Feature policy remains deployment state, not Canvas business state. No settings flag enters `CanvasSnapshot`, `MediaWorkflow`, `canvas/change`, layout events, Session Projection, or Browser local persistence.

Authorization and feature policy remain independent. UI hiding is not enforcement. Current Host entry points continue to use the same `ctx.canvasFeatures` checks, and future Agent Tools must advertise the current Host capability and re-check on execution.

The Canvas settings namespace contains booleans only. Provider credentials/secrets remain outside it; the settings wire is still invoked with redaction by the generic surface.

## Generated/lockfile boundary

The remediation introduces real workspace dependency edges to `@deepseek-ai/dsh-settings` and the optional client settings contract. `pnpm-lock.yaml` and generated Typert/client artifacts are not hand-edited. They must be regenerated by the repository-pinned toolchain when a functional runner is available.

## Repository validation blocker

Repository Actions are currently blocked before test execution across N06/N07/N08: standard `ubuntu-latest` jobs fail with zero steps and no log blob, while the repository Node 24 enterprise lanes remain queued on `dsh-ubuntu-24-04-16core`. This is runner/account allocation evidence, not a Canvas assertion failure.

N09 therefore remains `REVIEW`. No repository-wide typecheck, lint, build, GUI, generated-artifact, REAL composition, or coverage success is claimed until those checks run on this exact remediation head after the runner environment is restored or the existing repository failover path is used.

## Acceptance conclusion

N09 now has one complete authority chain: schema defaults and composition config feed the Harness `canvas` settings namespace, the durable user layer is sampled into Host deployment policy on activation, Browser Settings edits that same namespace, and current UI/Host enforcement consume only the effective Host capability snapshot. Historical disabled data remains readable and Canvas can be re-enabled from Settings even while its current product surface is disabled. Final status stays `REVIEW` pending repository-pinned validation and predecessor acceptance.